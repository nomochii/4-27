<%= render 'list' %>
<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
    <p>Title：<%= book.title %></p>
  <% end %>
  <p>Opinion：<%= book.opinion %></p>
  <p>name：<%= book.user.name %></p>
</div>
<% end %>